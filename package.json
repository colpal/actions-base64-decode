{
  "scripts": {
    "build": "esbuild app.js --bundle --platform=node --outfile=out.js"
  },
  "devDependencies": {
    "esbuild": "^0.12.19"
  },
  "dependencies": {
    "@actions/core": "^1.4.0"
  }
}
